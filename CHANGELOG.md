# 更新记录

## 0.2.6

- 修复 Java/C# 带缩进的单行 `/** ... */` 文档注释及 C# `///` 文档注释在只读视图有译文、源码视图仍显示原文的问题。
- 注释范围从实际注释符号开始，前导空格和 Tab 留在源码中；保留多行续行缩进、注释符号和准确坐标。
- 增加真实 TextMate 解析到源码渲染的回归验证，覆盖紧凑写法、文档标签、空格、Tab、CRLF 和相邻注释。

## 0.2.5

- 支持 HTML、XML 与 Vue 模板中的 `<!-- ... -->` 注释，保留原注释外壳、缩进和多行；排除属性字符串、CDATA、DOCTYPE、处理指令等非注释。
- 新增编辑器及资源管理器右键“翻译整个 Markdown 文件（只读）”，支持标题、列表、表格、代码块排版及原文/译文切换。
- Markdown 全文先查 SQLite，再按完整块批量翻译未命中内容；保护代码、链接目标和格式，不修改源文件。
- Markdown 仅手动触发；原文或配置变化后撤下旧译文并提示重新翻译，取消或关闭文件后不接受迟到结果。

## 0.2.4

- 源码视图改为在原注释位置逐行显示译文，悬停查看原文，不再在行尾追加整块译文。
- 保留原注释的行数、符号、缩进、空行及文档标签，译文不截断。
- 光标或选择范围进入注释所在行时恢复原文，离开后恢复译文；保存和复制仍使用原始源码。
- 同行多个注释分别替换，原文悬浮合并展示，保留注释前后的代码。
- 只读阅读视图继续使用上下对照与多行自由排版。

## 0.2.3

- 修复安装并配置后打开 Java 等受支持文件不自动翻译的问题，启动时自动激活扩展。
- 新增默认开启的 automatic 设置，配置完成后立即处理当前文件，开关跨重启保留。
- 配置向导期间暂停请求，完成后使用最终配置恢复；未配置或不受信任时显示明确提示。
- 修复在阅读视图中关闭再开启自动翻译时不能恢复译文的问题，继续复用 SQLite 缓存。
- 增加真实 VS Code 自动激活、Java 自动翻译、关闭与恢复自动模式的回归验证。

## 0.2.2

- 修复依赖锁文件中的不完整可选平台包记录，支持 GitHub Actions 干净安装与构建。
- 增加分支构建验证及版本标签自动发布，Release 提供 VSIX 和 SHA-256 校验文件。
- 补充 GitHub 仓库、问题反馈与安装链接。

## 0.2.1

- 译文保留原注释的 //、/** … */、* 等符号及相对缩进。
- 文档标签、参数名、类型与 XML 文档标签受到结构校验保护，失败项单独重试。
- 阅读视图与完整悬浮内容保留注释格式，行尾缩略预览保留闭合符号。
- 旧缓存经文档结构校验后复用，避免因提示词升级全量重新翻译。

## 0.2.0

- 移除原生评论框，新增无边框、多行自动换行的只读源码对照视图，原始行号可跳回源码。
- 原生编辑器保留单行译文预览和完整悬浮译文；显示方式可在设置中切换。
- 服务地址、模型名、API Key 均可在用户设置中自定义，继续支持加密存储 Key。
- 支持内网 HTTP 兼容服务，以及 max_tokens、max_completion_tokens 或省略 token 上限。
- 继续复用 SQLite 原文译文缓存，仅批量请求缺失注释。

## 0.1.0

- 独立行注释与文档注释上下对照，行尾注释追加译文。
- 按文件扫描，SQLite 查库后仅批量翻译未命中项。
- 支持 20 个常见代码语言模式，按 ID 校验批量翻译结果。
- 支持取消、有限重试、500 条自动额度及跨文件并发限制。
- API Key 加密存储，提供无需联网的离线效果示例。
